import { UsersSubscribedMealDtldPipe } from './users-subscribed-meal-dtld.pipe';

describe('UsersSubscribedMealDtldPipe', () => {
  it('create an instance', () => {
    const pipe = new UsersSubscribedMealDtldPipe();
    expect(pipe).toBeTruthy();
  });
});
